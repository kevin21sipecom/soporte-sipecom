# CLI

Tras `uv tool install`, el comando es global:

```text
sipecom-soporte --help
```

## (sin subcomando) / onboard

Runtime (node, npm), CodeGraph, Repomix, Archify y agentes. Pistas de instalación si falta npm u otra pieza.

## doctor

Las 4 familias, formato corto / `--json`.

## models

Listado vivo: `grok models`, `agy models`, `codex debug models`.

## dashboard

Streamlit en `127.0.0.1:2121`. Alias `ui`.

El detector rellena las CLIs; no hace falta `select`.
